<%= form_with(model: sprout, local: true) do |form| %>
  <% if sprout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sprout.errors.count, "error") %> prohibited this sprout from being saved:</h2>

      <ul>
      <% sprout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :store %>
    <%= form.text_field :store %>
  </div>

  <div class="field">
    <%= form.label :department %>
    <%= form.text_field :department %>
  </div>

  <div class="field">
    <%= form.label :employee %>
    <%= form.text_field :employee %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
